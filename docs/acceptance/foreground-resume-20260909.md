# 前台恢复修复验证

## 修改前

真实 Edge 页面，正常动效；使用 Playwright 时钟与 visibilitychange 模拟后台。同日恢复检测到分针先回退；跨日恢复检测到 midnight-reset-active。两项测试在原版失败，轨迹与截图保存在工作区 outputs/foreground-before-20260909。

## 修改

只修改 LIVE 状态的后台恢复路径：日期已变时更新日期基准，不补播错过的清零；三针、收入与进度共享 smootherStep 进度，目标随真实时间推进。工作中恢复用约 1.2–2.2 秒并对齐秒刻度，非工作约 1.2 秒。正常历史回溯、返回实时及实时走针保留。

## 验收

同日、跨日到上午、跨日到下午恢复三项测试通过，检查不补播清零、不回退、分针末段接近正常实时速度、无运行错误。完整本地质量检查：Edge 28 项、WebKit 2 项通过；既有截图基线不变，构建和 README 来源检查通过。

本地 Push 与手册校验值同步，小组件未改。手册仅更新校验值，本次未重做 Word 视觉验收。尚未上传 GitHub，未验证真实 iPhone 锁屏、系统杀进程或真实跨夜；模拟测试不代表这些环境已经通过。
