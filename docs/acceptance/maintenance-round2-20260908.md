# 第二轮维护记录

## 范围

四个交付文件、截图基线和 BlBl 不变。第一轮文档整理仍由独立 PR #27 承载。

## 已实现

- 输入与 WebKit 测试共用工作日业务时钟辅助函数；历史推进时钟不强行合并。
- 新增测试职责索引。
- Pages 改为 Quality 的可复用后续流程，只有 main 的 push 或手动 Quality 运行且三项检查成功才能部署。
- checkout 固定到同一 github.sha；保留发布文件逐字节校验。手动重部署应运行 main 的 Quality，不再直接运行 Pages。
- 新增部署结构静态约束测试；此测试不能替代合并后真实部署验收。

## 网格调查未完成

原截图在鼠标停止后采集，存在捕获回平状态的可能。尝试交互中采集画布时，工作日 14:30 的固定及持续前进时钟均触发原有空闲断言：800ms 内约 26–29 次清屏，阈值为 3。等待启动结束并延长等待也未消除。原因尚未确认，不能宣称只是截图测试问题。

已撤回所有网格试验改动，保留原测试与阈值，不更改页面绘制逻辑。后续需跟踪 body class、resize 与调度来源，再决定是否需要产品修复。

## 本地验收

最终工作树完整 quality:local 通过：Edge 25 项、WebKit 2 项、发布流程与新增部署结构测试、README 来源校验均通过。交付四文件及 SHA256SUMS 与 main 无差异。本次通过不消除上面特定工作时钟实验暴露的网格风险。

## Dependabot 更新评估

PR #24 更新 checkout v7.0.1、setup-node v7.0.0、setup-python v7.0.0。原 PR 的三项检查通过，但基于旧提交，且未验证新的 Pages 调用结构。保留独立 PR 和分支；本轮不合并、不关闭、不删除。应在部署流程通过后更新其基线，单独验证大版本变化。
